# Production build config for the path-mounted PWA (/pwa/, behind nginx).
# Read directly by Vite (vite build) — NOT via the shell, so leading-slash
# values are safe from MSYS2/Git-Bash path conversion.
VITE_API_BASE=/pwa/api/v1
VITE_ANCHOR_URL=/anchor/api/v1
